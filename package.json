{
  "name": "assignment",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd web && npm run dev\"",
    "install": "cd server && npm i && cd web && npm i"
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
